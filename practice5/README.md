# Practice 5: Python RegEx

## Overview
This project processes raw receipt data from EUROPHARMA and solves 10 common RegEx exercises.

## Files
- `receipt_parser.py`: Main script.
- `raw.txt`: Raw text data from the receipt.

## Usage
Run the script using:
```bash
python3 receipt_parser.py